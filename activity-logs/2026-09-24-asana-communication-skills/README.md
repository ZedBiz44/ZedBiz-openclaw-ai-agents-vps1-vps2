# Asana and communication skill revision and rollout

Date: 2026-09-24 Mountain Time | Agent: Cody | Status: Complete; real-assignment trial remains Jack's next step

Jack authorized four skill revisions and the exact deployment split in [issue 399](https://github.com/ZedBiz44/ZedBiz-openclaw-ai-agents-vps1-vps2/issues/399).

## Result

Agent Control and Communication installed and discovered on all 16 active agents. Advanced Control and Procedures installed and discovered on Amanda, Victor, Terry, Marsha and Ruby. All 42 requested installations match the pinned GitHub packages. OpenClaw discovery reports eligible, modelVisible and openclaw-workspace; Ruby's native Hermes skill tool discovers its four skills from /opt/data/skills. No gateway restart, model/credential changes or Asana mutations were made.

## Changes

Procedures now requires unnumbered verb-first titles, visible working order, a Deliverables block first, useful standalone subtasks, a complete example before repetition, and verified individual assignees after readiness checks. Authority routing no longer loops through the unrelated assigned-task execution workflow. Communication applies to all agents and human-facing task descriptions, retains Grade 8 English, and moves extended examples into a linked reference. Main-file word counts: Procedures 1664 -> 1420; Communication 2511 -> 799. The two authority skills received bounded clarifications rather than broad rewrites.

## Validation and pilot

Four runtime packages passed ZedBiz and native Codex validation, runtime links resolved, and Amanda's installed OpenClaw official validator passed all four final packages. Root-layout repository validation passed for the three applicable repositories; Procedures' runtime is nested. GitHub blob hashes were compared with the deployment manifest.

Amanda's isolated fresh-session tests read the installed skills and produced synthetic assignment drafts, without channel delivery or Asana calls. Initial numbered draft lists prompted an explicit bullet rule. Retest revealed ambiguous review/correction dependencies and a possible workaround for missing required dividers. The final targeted test correctly separates review response, corrections, final approval and delivery, and holds release for the missing required divider capability. Full test responses are preserved here. This proves tested drafting behavior, not live Asana ordering, divider support or future task quality. In the final test Amanda still requested due-date inputs; none were invented. Real assignments should use only relevant timing requirements.

## Evidence and recovery

See release-commits.json, manifest.json, verification.json, and the pilot records. baseline.json identifies previous canonical commits. Restore a skill from its prior GitHub source if required; never install backup copies into skill discovery roots. Existing non-runtime SKILL.md.before-task-memory-20260916 files were preserved and are not new release files.

The Python helpers are release-specific operator records, not runtime skill resources or general-purpose deployers. They assume the verified paths and SSH key filenames of this release and never contain credentials. Before any reuse, inspect current paths and state. Use a fresh agent conversation for the next trial so older skill contents already loaded in a long-running conversation are not mistaken for the current release.
