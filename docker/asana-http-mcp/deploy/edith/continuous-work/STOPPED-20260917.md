# Edith production stopped
Date: 2026-09-17 Mountain | Agent: Cody | Status: Stopped by Jack

Before stopping the Edith container, Cody added this exact admission guard to edith-project-dispatch.py:

```python
def dispatch(gid):
 if (ROOT/'production-paused-by-jack.json').exists():return {'status':'production-paused-by-jack','task':gid}
 connect();t=get_task(gid)
```

Backup: edith-project-dispatch.py.before-user-stop-20260917. Marker at /home/node/.openclaw/private/edith-dispatch/production-paused-by-jack.json records Jack's stop/investigate instruction. The continuous supervisor was suspended and then all Edith processes stopped by docker stop; no other agent container was stopped. Container state verified exited, Running=false, Pid=0 at03:15:44UTC. One scheduled backup task1218611357802915 cancelled and read back. Active task1218611177927226 left incomplete with explicit stop instructions.

Preserve mounted checkpoints, pending/uncertain journals, originals and output. No automatic restart. Any approved future restart first reconciles interrupted/uncertain writes and replaces the wasteful execution path. Do not simply remove the marker and restart the old loop.

Diagnosis: docs/incidents/2026-09-17-edith-stopped-efficiency-investigation.md
