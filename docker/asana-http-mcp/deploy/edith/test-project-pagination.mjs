import assert from 'node:assert/strict';
const get = new Function('return ({'+"async getTasksForProject(projectId, opts = {}) {\n    const rows = [], seen = new Set();\n    let offset = opts.offset;\n    for (let page = 0; page < 1000; page++) {\n      const response = await this.tasks.getTasksForProject(projectId, {...opts, limit: opts.limit ?? 100, ...(offset ? {offset} : {})});\n      if (!Array.isArray(response.data)) throw new Error(\"Invalid project task page\");\n      rows.push(...response.data);\n      offset = response.next_page?.offset;\n      if (!offset) return rows;\n      if (seen.has(offset)) throw new Error(\"Repeated project task cursor\");\n      seen.add(offset);\n    }\n    throw new Error(\"Project task pagination limit exceeded; no partial results returned\");\n  }"+'})')().getTasksForProject;
let calls=[];const obj={tasks:{getTasksForProject:async(_,o)=>{calls.push(o);return o.offset?{data:[{gid:'b'}],next_page:null}:{data:[{gid:'a'}],next_page:{offset:'two'}}}}};
assert.deepEqual(await get.call(obj,'123',{limit:1}),[{gid:'a'},{gid:'b'}]);assert.equal(calls.length,2);assert.equal(calls[1].offset,'two');
await assert.rejects(get.call({tasks:{getTasksForProject:async()=>({data:[],next_page:{offset:'same'}})}},'123',{}),/Repeated/);
let n=0;await assert.rejects(get.call({tasks:{getTasksForProject:async()=>{if(n++)throw Error('HTTP failure');return {data:[1],next_page:{offset:'two'}}}}},'123',{}),/HTTP failure/);
console.log('PASS multi-page, repeated cursor, and later-page failure without partial return');
