Jack authorized finishing the remaining six project folders. Complete FKLass then 4-Day-Build-A-Business in this single continuous Terra-medium session. There is no run time limit, backup task, restart or repeated wakeup. End naturally after both completed deliverables and review handoffs, or a concrete unresolved blocker. The old dispatcher must remain paused. Do not use it.

Read workspace/TOOLS.md, z-asana-agent-control and z-files-folders. Verify your own PAT Asana identity. Use the existing private per-project GOG registries, approved-shared account label. Both roots and inventories were verified under ZVIM by Cody. Do not search unrelated credentials. Preserve credentials holds and source formats.

FKLass: private/folder-cleanup/fklass; root 1e8yB0pBq2g8LnRMQunkh8if2Y7FS9-C1; parent Asana 1218559536949282; original Ruby approval 1218559622201055. Two DOCX files among 21 entries.
4-Day-Build-A-Business: private/folder-cleanup/4-day-build-a-business; root 1BEd21EgCO9UoJ3Hisof901Bfh6qZ98Oz; parent 1218559788863817; approval 1218559452895543. Two DOCX files among 21 entries.

Use each saved initial-inventory.json, ancestry.json and workload-assessment.json. Read actual DOCX contents once, classify using contents, then write the minimal correct file/folder plan. No blind filename-only classification or copying templates containing another root. Use the existing plan-review and execution_guard evidence schema (Laughs has a working example); self-review actual representative contents before writes. Keep a content-aware descriptive filename and correct extension. No conversions or deletion. Scope only the two stated roots.

Routine executor: workspace/scripts/folder_executor_template.py. Copy it into each project's private directory as execute-pilot.py; it derives its root from that directory's verified registry. workspace/scripts/folder_output_verification.py verifies binary source/output Drive MD5 and size, permissions and unchanged source metadata; saved actual source inspection is reused instead of downloading identical outputs again. Six tests passed. This is not another visual inspection; report that accurately. Native Google Docs deliberately stop in this template and are not present in these two folders.

Use normal work.gog and guarded execution only, never work._gog directly and never weaken guards. The first representative copy must pass before remaining file work. Preserve originals in project archive after verified active copies; save each returned ID/proof as usual. No force overwrite or uncertain-write replay.

Include all old first-level containers that should be retired in prewrite-plan.json folder_archive_plan, each with source_id, source_name, archive_path. Include the exact archive path in plan-review.json archive_paths. The installed original_folder_archive.py gate allows planned original first-level folders to move, IDs/names intact, into this root's direct Archive only AFTER every contained file is safely copied and archived and a live traversal shows only known original empty folders. It refuses files, protected items, unexpected contents or collisions. Six container tests and 14 execution_guard regressions passed. This is how to prevent old Research/Brand/Graphics containers being left beside the new organized homes. Do not invent a folder name change or bypass a refused move.

Finish each with one fresh full inventory: source preservation, active outputs, permissions, correct first/second-level names, obsolete containers archived, actual duplicates checked by content hash (size alone is candidates). Produce an honest completion manifest and shared proof pack under the project, then post the parent report with exact Drive links. Keep source inspection and historical evidence private; publish only appropriate deliverable proof. Ruby independently reviews all first/second-level folders, up to five file spot checks (all two here), and exact-duplicate candidates. Do not mark parent accepted yourself.

Release one complete ready-review task per folder using the existing meaningful ready_review_handoff helper if applicable. Read back assigned Ruby GID 1215900603267704. If its Asana rule leaves the review unassigned, direct assignment of the prepared complete review task to Ruby is authorized by Jack's request; use your verified Asana connection. Never make a second review task merely to retrigger. No polling tasks, deadline chain or time caps. Cody directly assigned the two prior correction-review tasks after their rule failed; do not repeat those handoffs.

Write a compact final result to private/edith-dispatch/small-folders-result.json containing each root, actual copied/archived counts, proof links, Ruby review task IDs, unresolved blockers and next action. This session ends after these two folder deliverables; the remaining four larger folders will receive their own content-based work scopes. Do not wait idle for Ruby before finishing your own assigned production deliverables.

