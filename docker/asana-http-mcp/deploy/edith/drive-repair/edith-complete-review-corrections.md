Jack authorized: fix the pickup, finish Laughs And Fun and InCanmore reviewer corrections, then continue the six remaining main folders with the tested continuous approach. This particular untimed session completes the two correction deliverables and their review handoffs. Use GPT-5.6 Terra medium. No short session limits, restarts, backup tasks, periodic wakeups or next-folder work in THIS correction session.

Read workspace/TOOLS.md: the existing private Drive registries are in private/folder-cleanup/laughs-and-fun/registry.json and private/folder-cleanup/incanmore/registry.json. Both passed GOG authentication/configuration preflight. Account-label approved-shared; client-code the matching folder slug. Do not search unrelated config or print registry contents. Read z-asana-agent-control and z-files-folders, verify your own PAT identity and current tasks before writes. Preserve all credential holds.

Laughs And Fun correction task 1218643489777657, parent 1218559788929226, Ruby approval 1218574348329943. Read Ruby's current exact correction. The seven leftover old first-level folders must move to the existing archive, retaining IDs and names. All file production is already complete; never recopy or re-inspect 778 files. The new reviewed_folder_corrections.py guard supports only those seven exact folder moves and five exact InCanmore renames. Its seven tests passed. For archive moves it verifies the leftover subtree contains only folders; if unexpected files remain, hold the affected move and report the exact IDs rather than bypassing.

Use the existing shared work.py helper by importing /home/node/.openclaw/private/folder-cleanup/albertawide/work.py. Set its P to the appropriate private project directory and ROOT from that project's verified registry BEFORE calls. Call work.gog('move', ID, '--parent', archiveID) or work.gog('rename', ID, exactNewName). work.gog now routes ONLY the exact approved corrections through the new tested guard. All other existing guards remain unchanged. Do not call work._gog directly or alter guards. The module's MOVES and RENAMES supply the exact allowed operations. Every mutation journals intent, verifies live ID/parent/name, and refuses blindly replaying an uncertain write. Use one imported process per project and preserve mutation locks.

InCanmore parent 1218559536996899, approval 1218559452275700. Read Ruby comment 1218607214439684; its five exact second-level folder renames are now supported. Leave parents, IDs, archive children, files, contents and permissions unchanged. Keep original pre-write evidence/history intact; save a correction overlay and update the current folder/path checkpoint to reflect the accepted renamed active homes. Do not present historical paths in the old proof pack as current without the correction overlay.

After each project's operations, verify the affected level-1/2 tree, precise IDs/names/parents, and no unintended new copies. Post one usable correction report on its parent with Drive links and exact resulting names/parents. Include the original acceptance ID and ask Ruby to recheck only the corrected placements/names; prior passing file samples remain valid. Complete the Laughs correction work task only after verification and report. Do not mark either main folder accepted yourself.

Release ONE meaningful correction-review assignment per corrected folder through the existing native Asana assignment-email rule. Use descriptive names 'Ruby — Recheck Laughs And Fun seven archived folders' and 'Ruby — Recheck InCanmore five folder names', include the complete exact expected IDs/results and original approval task, no timers or repeat checks. Check existing project tasks first and reuse an existing incomplete matching review instead of duplicating. Prepare unassigned with current due_at so the existing ready-review Rule assigns Ruby; verify readback. Existing ready_review_handoff.py deliberately refuses prior-review names, so do not loop or call it to manufacture another original review. Ruby records approved or changes_requested on the original approval and completes the parent only after accepted. A successful handoff is not acceptance.

Save a compact private final result at /home/node/.openclaw/private/edith-dispatch/review-corrections-result.json with outcomes, task IDs, exact blockers and proof paths, then end naturally. Cody will verify both handoffs and progress the remaining project work. No unrequested general skill edits, conversion, deletion, sharing or credentials work.

