interface:
  display_name: "ZedBiz Asana Agent Control"
  short_description: "Force correct Asana identity"
  default_prompt: "Use $zedbiz-asana-agent-control to verify the agent's Asana identity and work only through the PAT-based MCP path."

policy:
  allow_implicit_invocation: true
